# pnpm settings for the EngineerDNA monorepo
# Hoist nothing by default so workspace boundaries stay honest, but allow
# common build/tooling deps to be found by Next.js / NestJS toolchains.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
